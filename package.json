{
  "name": "aws-cleanup",
  "version": "1.0.0",
  "description": "Simple package that can delete unused AWS resources based on a given idle time.",
  "main": "index.ts",
  "scripts": {
    "preinstall": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "aws-cleanup": "./lib/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-ec2": "^3.554.0",
    "@aws-sdk/client-iam": "^3.554.0",
    "commander": "^12.0.0",
    "date-fns": "^3.6.0",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.12.7"
  }
}
